name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - name: Lint
        run: make lint
      - name: Test
        run: make test
      - name: Build
        run: make build
